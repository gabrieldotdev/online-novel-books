{
  "name": "www",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "lint:staged": "lint-staged",
    "format": "pnpm format:write && pnpm lint:fix",
    "format:write": "prettier --write \"**/*.{js,jsx,mdx}\" --cache",
    "format:check": "prettier --check \"**/*.{js,jsx,mdx}\" --cache"
  },
  "dependencies": {
    "next": "14.1.2",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.1",
    "esbuild": "^0.20.1",
    "eslint": "^8",
    "postcss": "^8",
    "tailwindcss": "^3.3.0"
  },
  "peerDependencies": {
    "esbuild": "^0.20.1"
  }
}
